% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_mixed_subjects_iterative}
\alias{fit_mixed_subjects_iterative}
\title{Fit a mixed-subjects 2PL calibration with iterative EM}
\usage{
fit_mixed_subjects_iterative(
  observed,
  predicted,
  generated,
  lambda = 1,
  n_quad = 31,
  initial_pars = NULL,
  quadrature = NULL,
  common_predicted_weights = TRUE,
  paired_missing = c("match_observed", "allow"),
  slope_lower = 1e-04,
  slope_upper = NULL,
  tol = 1e-04,
  em_maxit = 30,
  control = list(maxit = 200),
  ...
)
}
\arguments{
\item{observed}{Human response matrix, with rows for subjects and columns for
items. Values must be binary when \code{initial_pars} is omitted.}

\item{predicted}{Binary LLM responses (0/1) for the same rows and items as
\code{observed}. Probabilities are not accepted: fractional values are not a
valid likelihood input for the marginal IRT objective and break the PPI
correction, so sample binary responses from any probabilities first (e.g.
\code{rbinom}).}

\item{generated}{Binary generated or unlabeled LLM responses (0/1) for the
same item columns. Probabilities are not accepted (see \code{predicted}).}

\item{lambda}{Power-tuning parameter in \verb{[0, 1]}.}

\item{n_quad}{Number of standard-normal quadrature nodes.}

\item{initial_pars}{Optional starting item parameters. If omitted, a 2PL model
is fit to \code{observed}.}

\item{quadrature}{Optional quadrature grid with \code{theta} and \code{weight} columns.}

\item{common_predicted_weights}{Logical; if \code{TRUE}, reuse the observed human
posterior weights for \code{predicted}.}

\item{paired_missing}{How to handle missingness when
\code{common_predicted_weights = TRUE}. The default, \code{"match_observed"}, requires
\code{observed} and \code{predicted} to have the same missingness pattern so the paired
LLM correction is evaluated only where a human label is present. Use
\code{"allow"} only for explicit sensitivity analyses.}

\item{slope_lower}{Lower bound for discrimination parameters during
optimization. Use \code{NULL} for no lower bound.}

\item{slope_upper}{Upper bound on discrimination parameters. \strong{Strongly
recommended} when \code{lambda > 0} — the iterative EM updates posteriors at
each step, and without an upper bound the gradient asymmetry between
\code{L_pred} and \code{L_gen} can compound across iterations, driving
discrimination estimates to extreme values. A typical choice is
\code{slope_upper = 4} or \code{slope_upper = 6}.}

\item{tol}{Convergence tolerance: maximum absolute change in any parameter
across an EM iteration.}

\item{em_maxit}{Maximum number of EM iterations.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{...}{Additional arguments passed to \code{\link[=fit_2pl]{fit_2pl()}} when \code{initial_pars} is
omitted.}
}
\value{
An object of class \code{"mixedsubjects_fit"} with the standard fields
plus \code{em_iterations} (number of EM cycles completed) and \code{em_converged}
(logical).
}
\description{
Extends \code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}} by iterating the E-step and M-step until
convergence rather than fixing posterior quadrature weights at the initial
parameter estimates. At every iteration the posterior weights for all three
datasets (observed, predicted, generated) are recomputed using the same
current item parameters. This keeps the posteriors internally consistent and
avoids the asymmetry between \code{L_pred} and \code{L_gen} that arises when frozen
human-MLE weights are applied to LLM data with different item parameters.
}
\details{
\strong{Note on lambda selection.} This function accepts a fixed \code{lambda}. For
psychometric applications where accurate ability scoring is the goal, select
\code{lambda} with \code{\link[=tune_lambda_ability_risk]{tune_lambda_ability_risk()}} rather than \code{\link[=tune_lambda_ppi_score]{tune_lambda_ppi_score()}}.
The PPI++ score objective minimizes the trace of the item-parameter
covariance matrix; \code{\link[=tune_lambda_ability_risk]{tune_lambda_ability_risk()}} minimizes the propagated
ability-score risk \verb{g' Sigma g}, which is the quantity that matters for
downstream test scoring.
}
\examples{
set.seed(1)
pars <- data.frame(a = c(1, 1.2, 0.9), d = c(0, -0.5, 0.3))
observed <- simulate_2pl(rnorm(40), pars)
predicted <- observed
generated <- simulate_2pl(rnorm(100), pars)
fit <- fit_mixed_subjects_iterative(
  observed, predicted, generated,
  lambda = 0.5, initial_pars = pars, n_quad = 7,
  control = list(maxit = 50), em_maxit = 5
)
fit$item_pars
}
