% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{vcov_mixed_subjects}
\alias{vcov_mixed_subjects}
\title{Sandwich covariance for a mixed-subjects fit}
\usage{
vcov_mixed_subjects(object, ridge = 1e-08, ...)
}
\arguments{
\item{object}{A fitted object returned by \code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}} or
\code{\link[=fit_mixed_subjects_from_quadrature]{fit_mixed_subjects_from_quadrature()}} with response matrices and posterior
weights available in its quadrature summaries.}

\item{ridge}{Small ridge value used when inverting the Hessian.}

\item{...}{Unused; included for method compatibility.}
}
\value{
A covariance matrix with attributes \code{bread} and \code{meat}.
}
\description{
Estimates the full sandwich covariance matrix for item parameters from the
fixed-posterior expected-count estimating equations. The parameter order is
all discriminations followed by all intercepts, matching \code{fit$par}.
}
\examples{
set.seed(1)
pars <- data.frame(a = c(1, 1.2, 0.9), d = c(0, -0.5, 0.3))
observed <- simulate_2pl(rnorm(40), pars)
fit <- fit_mixed_subjects(
  observed, observed, simulate_2pl(rnorm(80), pars),
  lambda = 0.5, initial_pars = pars, n_quad = 7
)
dim(vcov_mixed_subjects(fit))
}
