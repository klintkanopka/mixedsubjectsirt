% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_mixed_subjects_mml}
\alias{fit_mixed_subjects_mml}
\title{Fit a mixed-subjects 2PL calibration via marginal maximum likelihood}
\usage{
fit_mixed_subjects_mml(
  observed,
  predicted,
  generated,
  lambda = 1,
  n_quad = 31,
  initial_pars = NULL,
  quadrature = NULL,
  mml_pred_weights = c("own", "human"),
  slope_lower = 1e-04,
  slope_upper = NULL,
  control = list(maxit = 500),
  ...
)
}
\arguments{
\item{observed}{Human response matrix, with rows for subjects and columns for
items. Values must be binary when \code{initial_pars} is omitted.}

\item{predicted}{Binary LLM responses (0/1) for the same rows and items as
\code{observed}. Probabilities are not accepted: fractional values are not a
valid likelihood input for the marginal IRT objective and break the PPI
correction, so sample binary responses from any probabilities first (e.g.
\code{rbinom}).}

\item{generated}{Binary generated or unlabeled LLM responses (0/1) for the
same item columns. Probabilities are not accepted (see \code{predicted}).}

\item{lambda}{Power-tuning parameter in \verb{[0, 1]}.}

\item{n_quad}{Number of standard-normal quadrature nodes.}

\item{initial_pars}{Optional starting item parameters. If omitted, a 2PL model
is fit to \code{observed}.}

\item{quadrature}{Optional quadrature grid with \code{theta} and \code{weight} columns.}

\item{mml_pred_weights}{How to compute posteriors for the paired \code{predicted}
term.  \code{"own"} uses posteriors from the predicted responses; \code{"human"}
uses posteriors from the observed human responses.  See Details.}

\item{slope_lower}{Lower bound for discrimination parameters during
optimization. Use \code{NULL} for no lower bound.}

\item{slope_upper}{Upper bound on discrimination parameters. Unlike
\code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}}, this function should not require capping for
well-posed problems because the true marginal objective has no false
minimum at large discrimination.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{...}{Additional arguments passed to \code{\link[=fit_2pl]{fit_2pl()}} when \code{initial_pars} is
omitted.}
}
\value{
An object of class \code{"mixedsubjects_fit"} with the same structure as
\code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}}.  For \strong{scalar} lambda fits, the quadrature
summaries store posteriors at the converged parameters, and
\code{stats::vcov()} dispatches automatically to
\code{\link[=vcov_mixed_subjects_mml]{vcov_mixed_subjects_mml()}} to compute the Louis-corrected marginal
sandwich covariance.  Calling \code{\link[=vcov_mixed_subjects]{vcov_mixed_subjects()}} directly bypasses
the Louis correction.  For \strong{vector} lambda fits, the summaries store
the frozen posteriors used during optimization, and \code{stats::vcov()}
dispatches to \code{\link[=vcov_mixed_subjects]{vcov_mixed_subjects()}} (EM bread) for consistency with the
frozen Q-function objective.
}
\description{
Estimates item parameters using the true IRT marginal likelihood for all
three loss terms.  Unlike \code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}}, which freezes posterior
quadrature weights at the initial parameter estimates before optimizing,
this function recomputes posterior weights at every gradient evaluation.
This eliminates the gradient asymmetry that causes \code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}} to
converge to false minima at inflated discrimination values when LLM item
parameters differ from human parameters.
}
\details{
\strong{Why it matters for lambda selection.}  With the frozen expected-count
implementation, the gradient of \code{L_pred} uses concentrated human posteriors
while \code{L_gen} uses diffuse LLM posteriors, making
\verb{grad(L_pred) >> grad(L_gen)} and systematically pushing discriminations
upward at any \code{lambda > 0}.  In the marginal-MML formulation all three terms
use their own current-parameter posteriors, so the asymmetry is absent at the
true optimum. As a result \code{\link[=tune_lambda_ability_risk]{tune_lambda_ability_risk()}} selects \code{lambda > 0}
whenever the LLM predictions are genuinely informative (e.g. \code{predicted = observed}), rather than collapsing to \code{lambda = 0} for all misaligned LLMs.

\strong{\code{mml_pred_weights}.}
\describe{
\item{\code{"own"} (default)}{L_pred uses posteriors computed from the
\emph{predicted} response matrix at the current parameter values.  All three
terms are true marginal likelihoods; objective and gradient are
internally consistent.  Recommended for most applications and required
for \code{\link[=vcov_mixed_subjects_mml]{vcov_mixed_subjects_mml()}} to produce the fully correct
Louis-formula bread.}
\item{\code{"human"}}{L_pred uses posteriors computed from the \emph{observed}
(human) response matrix, frozen at \code{initial_pars}.  This is a
\strong{fixed-nuisance Q-function}: the predicted term is treated as a frozen
expected-count lower bound rather than a true marginal likelihood.
Objective and gradient are mutually consistent (both use the same frozen
posteriors) so L-BFGS-B converges correctly.  Useful when strong
ability-level pairing is needed.  Note that \code{\link[=vcov_mixed_subjects_mml]{vcov_mixed_subjects_mml()}}
applies Louis' formula to the stored fixed posteriors, which is
approximately correct when \code{initial_pars} is close to \code{conv_pars}.}
}

\strong{Per-item lambda (vector \code{lambda}).}  When \code{lambda} is a length-\code{n_items}
vector rather than a scalar, \code{fit_mixed_subjects_mml} switches to a
\strong{frozen Q-function} objective: expected-count counts are computed once from
\code{initial_pars} and held fixed during L-BFGS-B, with item \code{j}'s counts
weighted by \code{lambda[j]}.  This is a consistent (objective, gradient) pair
but is \emph{not} the full marginal-MML objective — it is a frozen expected-count
approximation analogous to \code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}}.  Per-item lambda values
obtained from \code{\link[=tune_lambda_ability_risk_item]{tune_lambda_ability_risk_item()}} assign \code{lambda_j} near 0 to
items where the LLM correction is harmful, containing the frozen-posterior
gradient asymmetry.  Document per-item lambda results as approximate.
}
\examples{
set.seed(1)
pars <- data.frame(a = c(1, 1.2, 0.9), d = c(0, -0.5, 0.3))
observed  <- simulate_2pl(rnorm(40), pars)
generated <- simulate_2pl(rnorm(100), pars)
fit <- fit_mixed_subjects_mml(
  observed, observed, generated,
  lambda = 0.5, initial_pars = pars, n_quad = 7,
  control = list(maxit = 100)
)
fit$item_pars
}
