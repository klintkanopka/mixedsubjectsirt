% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadrature.R
\name{posterior_weights_2pl}
\alias{posterior_weights_2pl}
\title{Compute posterior quadrature weights for a 2PL model}
\usage{
posterior_weights_2pl(
  resp,
  item_pars,
  quadrature = NULL,
  n_quad = 31,
  iterlim = 1e+05
)
}
\arguments{
\item{resp}{A response matrix with rows for subjects and columns for items.
Values may be binary, fractional in \verb{[0, 1]}, or \code{NA}.}

\item{item_pars}{Item parameters in slope-intercept form. Supply a data frame
or matrix with columns \code{a}/\code{a1} and \code{d}, or a fitted \code{mirt} model.}

\item{quadrature}{Optional quadrature data frame with \code{theta} and \code{weight}
columns. If omitted, a standard-normal grid is created.}

\item{n_quad}{Number of quadrature nodes used when \code{quadrature} is omitted.}

\item{iterlim}{Maximum number of Newton-Raphson iterations passed to
\code{\link[rmutil:gauss.hermite]{rmutil::gauss.hermite()}} when \code{quadrature} is omitted.}
}
\value{
A matrix with one row per subject and one column per quadrature node.
Rows sum to one. Attributes \code{theta} and \code{weight} contain the grid.
}
\description{
Computes each subject's posterior distribution over a fixed quadrature grid
under a 2PL model, using stable log-likelihood calculations. Fractional
responses in \verb{[0, 1]} are allowed at this low level, which is useful when LLM
output is stored as probabilities rather than sampled binary responses.
}
\details{
Note: the high-level mixed-subjects fitting functions
(\code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} and relatives) require \strong{binary} \code{predicted} and
\code{generated}; fractional input is supported only in these low-level quadrature
utilities. If you have LLM-derived probabilities, sample binary responses from
them (e.g. with \code{\link[stats:Binomial]{stats::rbinom()}}) before calibrating.
}
\examples{
pars <- data.frame(a = c(1, 1.2), d = c(0, -0.5))
resp <- matrix(c(1, 0, 0, 1), nrow = 2, byrow = TRUE)
W <- posterior_weights_2pl(resp, pars, n_quad = 5)
rowSums(W)
}
