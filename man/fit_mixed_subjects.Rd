% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_mixed_subjects}
\alias{fit_mixed_subjects}
\title{Fit a mixed-subjects 2PL calibration}
\usage{
fit_mixed_subjects(
  observed,
  predicted,
  generated,
  lambda = 1,
  n_quad = 31,
  initial_pars = NULL,
  quadrature = NULL,
  common_predicted_weights = TRUE,
  slope_lower = 1e-04,
  control = list(maxit = 500),
  ...
)
}
\arguments{
\item{observed}{Human response matrix, with rows for subjects and columns for
items. Values must be binary when \code{initial_pars} is omitted.}

\item{predicted}{LLM responses or probabilities for the same rows and items as
\code{observed}.}

\item{generated}{Generated or unlabeled LLM responses or probabilities for
the same item columns.}

\item{lambda}{Power-tuning parameter in \verb{[0, 1]}.}

\item{n_quad}{Number of standard-normal quadrature nodes.}

\item{initial_pars}{Optional starting item parameters. If omitted, a 2PL model
is fit to \code{observed}.}

\item{quadrature}{Optional quadrature grid with \code{theta} and \code{weight} columns.}

\item{common_predicted_weights}{Logical; if \code{TRUE}, reuse the observed human
posterior weights for \code{predicted}.}

\item{slope_lower}{Lower bound for discrimination parameters during
optimization. Use \code{NULL} for no lower bound.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{...}{Additional arguments passed to \code{\link[=fit_2pl]{fit_2pl()}} when \code{initial_pars} is
omitted.}
}
\value{
An object of class \code{"mixedsubjects_fit"} with fitted \code{item_pars},
optimizer details, quadrature summaries, and input settings.
}
\description{
Fits item parameters using observed human responses, paired LLM
responses/predictions for those same subjects, and generated or unlabeled LLM
responses. This implements the expected-count objective
}
\details{
\code{L_human + lambda * (L_generated - L_paired_llm)}.

By default the paired LLM responses reuse the posterior quadrature weights
from the observed human responses. This keeps the paired human and LLM terms
on the same latent covariate distribution, which is the closest analog to
prediction-powered inference with paired labels.
}
\examples{
set.seed(1)
pars <- data.frame(a = c(1, 1.2, 0.9), d = c(0, -0.5, 0.3))
observed <- simulate_2pl(rnorm(40), pars)
predicted <- observed
generated <- simulate_2pl(rnorm(100), pars)
fit <- fit_mixed_subjects(
  observed, predicted, generated,
  lambda = 0.5, initial_pars = pars, n_quad = 7,
  control = list(maxit = 50)
)
fit$item_pars
}
