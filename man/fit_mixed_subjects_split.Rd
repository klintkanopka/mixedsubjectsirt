% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_mixed_subjects_split}
\alias{fit_mixed_subjects_split}
\title{Fit a split-sample mixed-subjects 2PL calibration}
\usage{
fit_mixed_subjects_split(
  observed,
  predicted,
  generated,
  lambda = 1,
  n_splits = 2,
  split_id = NULL,
  seed = NULL,
  n_quad = 31,
  initial_pars = NULL,
  quadrature = NULL,
  common_predicted_weights = TRUE,
  paired_missing = c("match_observed", "allow"),
  slope_lower = 1e-04,
  slope_upper = NULL,
  control = list(maxit = 500),
  ...
)
}
\arguments{
\item{observed}{Human response matrix, with rows for subjects and columns for
items. Values must be binary when \code{initial_pars} is omitted.}

\item{predicted}{Binary LLM responses (0/1) for the same rows and items as
\code{observed}. Probabilities are not accepted: fractional values are not a
valid likelihood input for the marginal IRT objective and break the PPI
correction, so sample binary responses from any probabilities first (e.g.
\code{rbinom}).}

\item{generated}{Binary generated or unlabeled LLM responses (0/1) for the
same item columns. Probabilities are not accepted (see \code{predicted}).}

\item{lambda}{Power-tuning parameter in \verb{[0, 1]}. Supply a scalar for a fixed
lambda or a vector with one value per split for a precomputed
cross-fitted-lambda analysis. When a vector is supplied, the generated term
uses the split-size weighted mean lambda.}

\item{n_splits}{Number of sample splits.}

\item{split_id}{Optional integer vector assigning each observed row to a
split. If omitted, splits are sampled at random.}

\item{seed}{Optional random seed used when \code{split_id} is omitted.}

\item{n_quad}{Number of standard-normal quadrature nodes.}

\item{initial_pars}{Optional item parameters to use in every fold instead of
fitting fold-specific human models. This is mainly useful for testing or
sensitivity analyses.}

\item{quadrature}{Optional quadrature grid with \code{theta} and \code{weight} columns.}

\item{common_predicted_weights}{Logical; if \code{TRUE}, reuse each held-out
observed posterior weight matrix for its paired LLM responses.}

\item{paired_missing}{How to handle missingness when
\code{common_predicted_weights = TRUE}. The default, \code{"match_observed"}, requires
\code{observed} and \code{predicted} to have the same missingness pattern.}

\item{slope_lower}{Lower bound for discrimination parameters during
optimization. Use \code{NULL} for no lower bound.}

\item{slope_upper}{Upper bound for discrimination parameters during
optimization. Use \code{NULL} (the default) for no upper bound.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{...}{Additional arguments passed to \code{\link[=fit_2pl]{fit_2pl()}} when fold-specific
initial models are fit.}
}
\value{
An object of class \code{"mixedsubjects_fit"} with \code{split} metadata and
fold-level initial parameters.
}
\description{
Fits the same objective as \code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}}, but constructs labeled
expected counts with cross-fitted posterior weights. For each split, the
initial human 2PL model is fit on the other splits and then used to compute
posterior weights for the held-out split. Each human row contributes to the
final estimating equation exactly once.
}
\details{
Generated LLM counts are computed once per fold and averaged across folds so
that the generated sample keeps its original sample-size scale.
}
\examples{
set.seed(2)
pars <- data.frame(a = c(1, 1.2, 0.9), d = c(0, -0.5, 0.3))
observed <- simulate_2pl(rnorm(40), pars)
predicted <- observed
generated <- simulate_2pl(rnorm(100), pars)
fit <- fit_mixed_subjects_split(
  observed, predicted, generated,
  lambda = 0.5, initial_pars = pars, n_splits = 2,
  n_quad = 7, control = list(maxit = 50)
)
fit$item_pars
}
