% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-1pl.R
\name{fit_1pl}
\alias{fit_1pl}
\title{Fit a 1PL (one-parameter logistic) model}
\usage{
fit_1pl(
  resp,
  n_quad = 31,
  initial_pars = NULL,
  quadrature = NULL,
  slope_lower = 1e-04,
  slope_upper = NULL,
  control = list(maxit = 500)
)
}
\arguments{
\item{resp}{Binary response matrix.}

\item{n_quad}{Number of standard-normal quadrature nodes.}

\item{initial_pars}{Optional starting item parameters (data frame with \code{a}
and \code{d} columns). If omitted, \code{a = 1} and \code{d_j = qlogis(p_j)} where
\code{p_j} is the observed proportion correct for item \code{j}.}

\item{quadrature}{Optional quadrature grid.}

\item{slope_lower, slope_upper}{Bounds on the shared discrimination.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
A list with \code{pars} (item parameter data frame with all \code{a} equal),
\code{par} (the raw parameter vector), and optimizer details.
}
\description{
Estimates a shared discrimination parameter \code{a} (equal across all items)
and per-item intercepts \code{d_j} by maximizing the IRT marginal likelihood
under a standard-normal ability prior using L-BFGS-B.
}
\details{
The response probability is \code{P(x_j = 1 | theta) = plogis(a * theta + d_j)}.
The parameter vector has length \code{J + 1}: one shared discrimination followed
by J per-item intercepts.
}
\examples{
set.seed(1)
pars <- data.frame(a = 1, d = c(-0.5, 0, 0.5))
resp <- simulate_2pl(rnorm(60), pars)
fit <- fit_1pl(resp, n_quad = 7)
fit$pars
}
