% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{link_item_parameters}
\alias{link_item_parameters}
\title{Link item parameters onto a target scale}
\usage{
link_item_parameters(source, target, method = c("mean_mean", "none"))
}
\arguments{
\item{source}{Item parameters to transform. A matrix or data frame with
columns \code{a}/\code{a1} and \code{d}, or a fitted \code{mirt} model.}

\item{target}{Item parameters defining the target scale. Uses the same
accepted formats as \code{source}.}

\item{method}{Linking method. Currently \code{"mean_mean"} and \code{"none"} are
supported.}
}
\value{
A list with transformed \code{pars}, linking constants \code{A} and \code{B}, and
the selected \code{method}.
}
\description{
Applies mean-mean linking to express source item parameters on the scale of a
target calibration. Both parameter sets must be in slope-intercept form for
the model \code{plogis(d + a * theta)}.
}
\details{
If \code{theta_target = A * theta_source + B}, then source parameters transform as
\code{a_target = a_source / A} and \code{b_target = A * b_source + B}, with
\code{d_target = -a_target * b_target}. Mean-mean linking chooses \code{A} and \code{B} so
that the transformed source parameters match the target mean discrimination
and mean difficulty.
}
\examples{
source <- data.frame(a = c(0.8, 1.2), d = c(-0.2, 0.5))
target <- data.frame(a = c(1.0, 1.5), d = c(-0.1, 0.4))
link_item_parameters(source, target)$pars
}
