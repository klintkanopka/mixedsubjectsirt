% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{mixed_subjects_loss}
\alias{mixed_subjects_loss}
\title{Mixed-subjects objective function}
\usage{
mixed_subjects_loss(pars, q_observed, q_predicted, q_llm, lambda = 0)
}
\arguments{
\item{pars}{Numeric vector of item parameters: all discriminations \code{a}
followed by all intercepts \code{d}.}

\item{q_observed}{Quadrature summary for observed human responses, usually
returned by \code{\link[=mixed_subjects_quadrature]{mixed_subjects_quadrature()}}.}

\item{q_predicted}{Quadrature summary for LLM responses/predictions on the
same labeled human subjects.}

\item{q_llm}{Quadrature summary for generated or unlabeled LLM responses.}

\item{lambda}{Power-tuning parameter in \verb{[0, 1]}.}
}
\value{
A scalar loss.
}
\description{
Evaluates the rectified mixed-subjects loss for 2PL item parameters. The
parameter vector must contain all discriminations first, followed by all
intercepts. The response probability is \code{plogis(d + a * theta)}.
}
\details{
The objective is
\code{L_observed(pars) + lambda * (L_generated(pars) - L_predicted(pars))}.
Setting \code{lambda = 0} gives the human-only expected-count objective.
}
\examples{
pars <- data.frame(a = c(1, 1.2), d = c(0, -0.5))
resp <- matrix(c(1, 0, 0, 1), nrow = 2, byrow = TRUE)
q <- mixed_subjects_quadrature(resp, item_pars = pars, N_quad = 5)
mixed_subjects_loss(c(pars$a, pars$d), q, q, q, lambda = 0.5)
}
