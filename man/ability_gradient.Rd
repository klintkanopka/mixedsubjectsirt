% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{ability_gradient}
\alias{ability_gradient}
\title{Gradient of ML ability scores with respect to item parameters}
\usage{
ability_gradient(resp, item_pars, theta = NULL, bounds = c(-6, 6), eps = 1e-10)
}
\arguments{
\item{resp}{Response matrix with rows for subjects and columns for items.}

\item{item_pars}{Item parameters in slope-intercept form, or a
\code{"mixedsubjects_fit"} object.}

\item{theta}{Optional precomputed ability estimates. If omitted,
\code{\link[=score_theta]{score_theta()}} is used.}

\item{bounds}{Bounds passed to \code{\link[=score_theta]{score_theta()}} when \code{theta} is omitted.}

\item{eps}{Tolerance used to mark near-zero test information as undefined.}
}
\value{
A matrix with one row per response pattern and one column per item
parameter.
}
\description{
Computes the implicit derivative of bounded maximum-likelihood ability scores
with respect to 2PL item parameters. The column order is all discriminations
followed by all intercepts.
}
\examples{
pars <- data.frame(a = c(1, 1.2), d = c(0, -0.5))
resp <- matrix(c(1, 0, 0, 1), nrow = 2, byrow = TRUE)
ability_gradient(resp, pars)
}
