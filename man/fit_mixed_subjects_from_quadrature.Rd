% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_mixed_subjects_from_quadrature}
\alias{fit_mixed_subjects_from_quadrature}
\title{Fit from precomputed quadrature summaries}
\usage{
fit_mixed_subjects_from_quadrature(
  q_observed,
  q_predicted,
  q_generated,
  lambda = 1,
  initial_pars = NULL,
  slope_lower = 1e-04,
  slope_upper = NULL,
  control = list(maxit = 500)
)
}
\arguments{
\item{q_observed}{Quadrature summary for observed human responses. Usually
returned by \code{\link[=mixed_subjects_quadrature]{mixed_subjects_quadrature()}}, but a raw counts object returned
by \code{\link[=summarize_expected_counts]{summarize_expected_counts()}} is also accepted.}

\item{q_predicted}{Quadrature summary for paired LLM responses/predictions on
the labeled human rows.}

\item{q_generated}{Quadrature summary for generated or unlabeled LLM
responses.}

\item{lambda}{Power-tuning parameter in \verb{[0, 1]}.}

\item{initial_pars}{Starting item parameters in slope-intercept form. If
omitted, \code{q_observed$irt_pars} is used when available.}

\item{slope_lower}{Lower bound for discrimination parameters during
optimization. Use \code{NULL} for no lower bound.}

\item{slope_upper}{Upper bound for discrimination parameters during
optimization. Use \code{NULL} (the default) for no upper bound.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
An object of class \code{"mixedsubjects_fit"}.
}
\description{
Fits the mixed-subjects 2PL objective from quadrature/count summaries rather
than raw response matrices. This lower-level interface is useful when the
human, paired LLM, and generated LLM summaries have already been linked onto a
common scale outside the package.
}
\examples{
pars <- data.frame(a = c(1, 1.2), d = c(0, -0.5))
resp <- matrix(c(1, 0, 0, 1), nrow = 2, byrow = TRUE)
q <- mixed_subjects_quadrature(resp, item_pars = pars, N_quad = 5)
fit_mixed_subjects_from_quadrature(q, q, q, lambda = 0.5)$item_pars
}
