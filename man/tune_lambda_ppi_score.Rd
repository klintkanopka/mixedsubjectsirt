% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{tune_lambda_ppi_score}
\alias{tune_lambda_ppi_score}
\title{Plug-in PPI++ optimal tuning parameter}
\usage{
tune_lambda_ppi_score(
  observed,
  predicted,
  item_pars,
  n_generated,
  quadrature = NULL,
  n_quad = 31
)
}
\arguments{
\item{observed}{Human response matrix.}

\item{predicted}{Paired binary LLM responses (0/1) for the same rows as
\code{observed}. Probabilities are not accepted; sample binary responses first.}

\item{item_pars}{Item parameters in slope-intercept form at which to
evaluate the score vectors. Typically the human 2PL MLE from \code{\link[=fit_2pl]{fit_2pl()}}.}

\item{n_generated}{Number of generated (unpaired) LLM subjects, used to
compute the ratio \code{r} (\code{n / n_generated}).}

\item{quadrature}{Optional quadrature grid. If omitted, a standard-normal
grid with \code{n_quad} nodes is created.}

\item{n_quad}{Number of quadrature nodes when \code{quadrature} is omitted.}
}
\value{
A list with elements \code{lambda} (the plug-in estimate, clipped to
[0, 1]), \code{n}, \code{n_generated}, \code{r}, and the intermediate matrices \code{C_hf}
(cross-covariance of human and paired-LLM score vectors) and \code{V_f}
(variance of paired-LLM score vectors).
}
\description{
Implements the closed-form estimator from Proposition 2 of Angelopoulos,
Duchi and Zrnic (2023) for the lambda that minimizes the trace of the
asymptotic item-parameter covariance matrix \code{Tr(Sigma_gamma)}.
}
\details{
\strong{This is the item-parameter variance objective, not the psychometric
scoring objective.} For IRT applications where accurate ability scoring
is the goal, use \code{\link[=tune_lambda_ability_risk]{tune_lambda_ability_risk()}} or
\code{\link[=tune_lambda_ability_risk_crossfit]{tune_lambda_ability_risk_crossfit()}} instead. Those functions directly
minimize the propagated ability-score risk \verb{E[g' Sigma_gamma g]} — the
quantity that matters for test scoring — rather than item-parameter
estimation efficiency. \code{tune_lambda_ppi_score()}
is provided as a theoretical diagnostic and to facilitate method validation.

The formula uses the \strong{same} human posterior weights for both the human and
paired-LLM score vectors. This symmetry is required for the PPI++
unbiasedness condition \code{E[grad_gen] = E[grad_pred]} at the true parameters.
}
\examples{
set.seed(1)
pars <- data.frame(a = c(1, 1.2, 0.9), d = c(0, -0.5, 0.3))
observed <- simulate_2pl(rnorm(40), pars)
predicted <- observed
tune_lambda_ppi_score(observed, predicted, pars, n_generated = 100, n_quad = 7)$lambda
}
