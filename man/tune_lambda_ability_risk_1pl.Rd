% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk-1pl.R
\name{tune_lambda_ability_risk_1pl}
\alias{tune_lambda_ability_risk_1pl}
\title{Tune lambda by downstream ability-score risk for a 1PL model}
\usage{
tune_lambda_ability_risk_1pl(
  lambda_grid = seq(0, 1, by = 0.1),
  observed,
  predicted,
  generated,
  target_resp = NULL,
  theta_true = NULL,
  n_quad = 31,
  initial_pars = NULL,
  fit_fn = fit_mixed_subjects_mml_1pl,
  method = c("optimize", "grid"),
  bounds = c(-6, 6),
  max_discrimination = 10,
  control = list(maxit = 500),
  ...
)
}
\arguments{
\item{lambda_grid}{Numeric vector of candidate lambda values in \verb{[0, 1]}. For
\code{method = "grid"} these are the evaluated candidates; for
\code{method = "optimize"} only \code{range(lambda_grid)} matters and bounds the search
(e.g. \code{lambda_grid = c(0, 0.8)} caps lambda at 0.8). Defaults to
\code{seq(0, 1, by = 0.1)}.}

\item{observed, predicted, generated}{Response matrices passed to
\code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}}.}

\item{target_resp}{Response matrix defining the target scoring population. If
omitted, \code{observed} is used.}

\item{theta_true}{Optional true theta values for \code{target_resp}, used in
simulation studies to add squared scoring error to the risk. When omitted,
\code{mean_squared_error} in the summary is \code{NA}; only \code{mean_param_var} is
computed.}

\item{n_quad}{Number of quadrature nodes.}

\item{initial_pars}{Optional starting item parameters.}

\item{fit_fn}{Fitting function. Defaults to \code{\link[=fit_mixed_subjects_mml_1pl]{fit_mixed_subjects_mml_1pl()}}.}

\item{method}{How lambda is chosen: \code{"optimize"} (default, direct 1-D
optimization over \code{range(lambda_grid)}, continuous lambda) or \code{"grid"}
(evaluate every value in \code{lambda_grid} and take the argmin).}

\item{bounds}{Bounds passed to \code{\link[=score_theta]{score_theta()}}.}

\item{max_discrimination}{Upper bound on plausible item discrimination. Any
candidate fit whose maximum \verb{|a|} exceeds this value is treated as
degenerate and excluded from selection. This guards against runaway
discrimination fits, which can "converge" with a spuriously low model-based
risk (huge discrimination collapses the item-parameter covariance). The
default of 10 is far above any realistic 2PL discrimination.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{...}{Additional arguments passed to \code{fit_fn}.}
}
\value{
A list with \code{summary}, \code{best_lambda}, \code{best_fit}, \code{fits}, \code{risks}.
}
\description{
Selects the lambda minimizing \verb{E[g' Sigma_1pl g]} — the propagated
ability-score risk in the 1PL parameterization — using
\code{\link[=fit_mixed_subjects_mml_1pl]{fit_mixed_subjects_mml_1pl()}} by default. As in the 2PL
\code{\link[=tune_lambda_ability_risk]{tune_lambda_ability_risk()}}, lambda is chosen by direct 1-D optimization
(\code{method = "optimize"}, the default) or over \code{lambda_grid}
(\code{method = "grid"}).
}
\details{
Passes \code{fit_fn} to allow switching between the frozen expected-count
estimator (\code{\link[=fit_mixed_subjects_1pl]{fit_mixed_subjects_1pl()}}) and the marginal-MML estimator
(\code{\link[=fit_mixed_subjects_mml_1pl]{fit_mixed_subjects_mml_1pl()}}).
}
\examples{
set.seed(1)
pars <- data.frame(a = 1, d = c(-0.5, 0, 0.5))
obs  <- simulate_2pl(rnorm(40), pars)
gen  <- simulate_2pl(rnorm(100), pars)
tuned <- tune_lambda_ability_risk_1pl(
  c(0, 0.5), obs, obs, gen,
  initial_pars = pars, n_quad = 5, control = list(maxit = 30)
)
tuned$best_lambda
}
\seealso{
\code{\link[=tune_lambda_ability_risk]{tune_lambda_ability_risk()}} for the 2PL version;
\code{\link[=tune_lambda_ppi_score_1pl]{tune_lambda_ppi_score_1pl()}} for the PPI++ score diagnostic.
}
