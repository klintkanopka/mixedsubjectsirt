% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{tune_lambda_ppi_score_item}
\alias{tune_lambda_ppi_score_item}
\title{Per-item PPI++ optimal tuning parameters}
\usage{
tune_lambda_ppi_score_item(
  observed,
  predicted,
  item_pars,
  n_generated,
  quadrature = NULL,
  n_quad = 31
)
}
\arguments{
\item{observed}{Human response matrix.}

\item{predicted}{Paired binary LLM responses (0/1) for the same rows as
\code{observed}. Probabilities are not accepted; sample binary responses first.}

\item{item_pars}{Item parameters at which to evaluate the score vectors.}

\item{n_generated}{Number of generated (unpaired) LLM subjects.}

\item{quadrature}{Optional quadrature grid.}

\item{n_quad}{Number of quadrature nodes when \code{quadrature} is omitted.}
}
\value{
A list with \code{lambda} (numeric vector of length \code{n_items}), \code{item}
(item names), \code{n}, \code{n_generated}, and \code{r} (the ratio \code{n / n_generated}).
}
\description{
Applies the PPI++ Proposition 2 plug-in formula independently for each item,
producing a vector of item-specific lambda values \verb{λ_j ∈ [0, 1]}.
}
\details{
The global \code{\link[=tune_lambda_ppi_score]{tune_lambda_ppi_score()}} uses the full parameter covariance matrix
\code{Tr(Σ_γ)} as the objective. This function instead applies the same formula
using only the 2×2 diagonal block of the inverse Hessian for item \code{j}, and
the 2D sub-vectors of the human and paired-LLM score vectors. The result is
the λ that minimizes the marginal variance of \verb{(a_j, d_j)} independently for
each item.

\strong{Use case.} When a single global λ is forced to zero because a few items
have poor LLM predictions, per-item λ_j allows well-predicted items to still
benefit from the LLM data. Pass the returned vector to
\code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} as the \code{lambda} argument.

This is a \strong{theoretical diagnostic}: it minimizes item-parameter variance,
not ability-score risk. For operational scoring use
\code{\link[=tune_lambda_ability_risk_item]{tune_lambda_ability_risk_item()}} instead.
}
\examples{
set.seed(1)
pars <- data.frame(a = c(1, 1.2, 0.9), d = c(0, -0.5, 0.3))
observed <- simulate_2pl(rnorm(40), pars)
tune_lambda_ppi_score_item(observed, observed, pars, n_generated = 100, n_quad = 7)$lambda
}
\seealso{
\code{\link[=tune_lambda_ppi_score]{tune_lambda_ppi_score()}} for the global version;
\code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} to fit with a per-item lambda vector.
}
