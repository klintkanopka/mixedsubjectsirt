% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{tune_lambda_ability_risk_crossfit}
\alias{tune_lambda_ability_risk_crossfit}
\title{Cross-fit ability-score-risk lambda tuning}
\usage{
tune_lambda_ability_risk_crossfit(
  lambda_grid = seq(0, 1, by = 0.1),
  observed,
  predicted,
  generated,
  target_resp = NULL,
  theta_true = NULL,
  n_splits = 2,
  split_id = NULL,
  seed = NULL,
  n_quad = 31,
  initial_pars = NULL,
  target_mode = c("fixed", "row_aligned"),
  fit_fn = fit_mixed_subjects,
  final_fit_fn = fit_mixed_subjects_split,
  tuning_args = list(),
  final_args = list(),
  bounds = c(-6, 6),
  control = list(maxit = 500),
  ...
)
}
\arguments{
\item{lambda_grid}{Numeric vector of candidate lambda values in \verb{[0, 1]}. For
\code{method = "grid"} these are the evaluated candidates; for
\code{method = "optimize"} only \code{range(lambda_grid)} matters and bounds the search
(e.g. \code{lambda_grid = c(0, 0.8)} caps lambda at 0.8). Defaults to
\code{seq(0, 1, by = 0.1)}.}

\item{observed, predicted, generated}{Response matrices passed to
\code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}}.}

\item{target_resp}{Response matrix defining the target scoring population. If
omitted, \code{observed} is used.}

\item{theta_true}{Optional true theta values for \code{target_resp}, used in
simulation studies to add squared scoring error to the risk. When omitted,
\code{mean_squared_error} in the summary is \code{NA}; only \code{mean_param_var} is
computed.}

\item{n_splits}{Number of sample splits.}

\item{split_id}{Optional integer split assignment for labeled rows.}

\item{seed}{Optional seed used when \code{split_id} is omitted.}

\item{n_quad}{Number of quadrature nodes.}

\item{initial_pars}{Optional starting item parameters.}

\item{target_mode}{How \code{target_resp} is handled in each fold.
\code{"fixed"} (default): the full \code{target_resp} is used to evaluate risk in
every fold, suitable when the target population is fixed and independent of
the labeled-data split (e.g. an operational scoring population).
\code{"row_aligned"}: only the training rows of \code{target_resp} are used, which
is valid when \code{target_resp = observed} and fold-matched evaluation is
desired.}

\item{fit_fn}{Fitting function used for each fold's ability-risk tuning
(passed to \code{\link[=tune_lambda_ability_risk]{tune_lambda_ability_risk()}}). Defaults to
\code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}} (frozen expected-count). Pass
\code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} for marginal-MML fold tuning.}

\item{final_fit_fn}{Function used to produce the final combined-data fit.
Defaults to \code{\link[=fit_mixed_subjects_split]{fit_mixed_subjects_split()}}, which accepts a per-fold
\code{lambda} vector natively.
Pass \code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} to get a scalar marginal-MML final fit: the
fold-specific lambdas are averaged (weighted by fold size) into a single
scalar, avoiding the accidental per-item lambda problem that occurs when a
length-\code{n_splits} vector is passed directly to \code{fit_mixed_subjects_mml()}.
Note that mixing MML fold-tuning with a frozen final fit is an
approximation; document this when reporting results.}

\item{tuning_args}{Named list of extra arguments forwarded only to the
fold-level \code{\link[=tune_lambda_ability_risk]{tune_lambda_ability_risk()}} calls (and through them to
\code{fit_fn}). For example, \code{tuning_args = list(slope_upper = 4)}.}

\item{final_args}{Named list of extra arguments forwarded only to
\code{final_fit_fn}. For example, \code{final_args = list(mml_pred_weights = "own")}.
This keeps tuning-specific and final-fit-specific arguments cleanly
separated, avoiding the earlier \code{...} leakage between the two.}

\item{bounds}{Bounds passed to \code{\link[=score_theta]{score_theta()}}.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{...}{Deprecated; forwarded to \code{tuning_args} for backward compatibility
with a one-time message. Prefer \code{tuning_args} / \code{final_args}.}
}
\value{
A list with fold-specific lambda values, fold tuning objects, and the
final fit.
}
\description{
Estimates lambda separately for each held-out split using only the remaining
labeled rows, then fits a final model with those fold-specific lambda values.
}
