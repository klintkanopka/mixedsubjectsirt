% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{vcov_mixed_subjects_mml}
\alias{vcov_mixed_subjects_mml}
\title{Marginal-MML sandwich covariance for a mixed-subjects fit}
\usage{
vcov_mixed_subjects_mml(object, ridge = 1e-08, ...)
}
\arguments{
\item{object}{A scalar-lambda \code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} fit.}

\item{ridge}{Ridge regularization for bread inversion.}

\item{...}{Unused.}
}
\value{
A \eqn{2J \times 2J} covariance matrix with attributes \code{bread} and \code{meat}.
}
\description{
Computes the full sandwich covariance for the scalar marginal-MML PPI++
estimator from \code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}}.  The bread uses Louis's (1982)
observed marginal-information formula
}
\details{
\deqn{A_\lambda^\mathrm{marg} = H_\lambda^\mathrm{comp} - I_\lambda^\mathrm{miss}}

rather than the EM/complete-data Hessian used by \code{\link[=vcov_mixed_subjects]{vcov_mixed_subjects()}}.
Using the complete-data Hessian as the bread for a marginal-MML estimator
would over-state efficiency by ignoring the missing-information correction.

The meat uses the standard marginal per-person score vectors (posteriors at
the converged parameters), which is identical to \code{\link[=vcov_mixed_subjects]{vcov_mixed_subjects()}}.

\strong{When is this function called automatically?} The \code{vcov()} method for
\code{"mixedsubjects_fit"} objects (see \code{\link[stats:vcov]{stats::vcov()}}) dispatches here whenever
\code{isTRUE(object$mml) && length(object$lambda) == 1}. For vector-lambda fits, or
for frozen expected-count fits, the existing \code{\link[=vcov_mixed_subjects]{vcov_mixed_subjects()}} is used.
}
\seealso{
\code{\link[=vcov_mixed_subjects]{vcov_mixed_subjects()}} for the frozen expected-count version. The
internal \code{louis_missing_info()} helper computes the missing-information
correction.
}
