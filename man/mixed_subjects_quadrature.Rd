% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadrature.R
\name{mixed_subjects_quadrature}
\alias{mixed_subjects_quadrature}
\title{Convert responses to quadrature form}
\usage{
mixed_subjects_quadrature(
  resp,
  N_quad = 31,
  eps = 1e-15,
  iterlim = 1e+05,
  irt_pars = NULL,
  item_pars = NULL,
  quadrature = NULL,
  link_method = "mean_mean",
  ...
)
}
\arguments{
\item{resp}{A response matrix with rows for subjects and columns for items.}

\item{N_quad}{Number of quadrature nodes to compute. Kept for backward
compatibility; prefer \code{n_quad} in new code.}

\item{eps}{Retained for backward compatibility. Stable log computations are
used instead of probability clipping.}

\item{iterlim}{Maximum number of Newton-Raphson iterations passed to
\code{\link[rmutil:gauss.hermite]{rmutil::gauss.hermite()}}.}

\item{irt_pars}{Optional target item parameters for mean-mean linking. This
argument is kept for backward compatibility with earlier package versions.}

\item{item_pars}{Optional item parameters. If omitted, a 2PL model is fit to
\code{resp} using \code{\link[=fit_2pl]{fit_2pl()}}.}

\item{quadrature}{Optional quadrature grid with \code{theta} and \code{weight} columns.}

\item{link_method}{Linking method used when \code{irt_pars} is supplied.}

\item{...}{Additional arguments passed to \code{\link[=fit_2pl]{fit_2pl()}} when \code{item_pars} is
omitted.}
}
\value{
A list with \code{quad}, \code{counts}, \code{weights}, \code{irt_pars}, \code{quadrature}, and
\code{theta}.
}
\description{
Fits or accepts a 2PL model, computes posterior quadrature weights for each
subject, and returns expected counts for mixed-subjects calibration. This is a
lower-level helper; most analyses should call \code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}} or
\code{\link[=fit_mixed_subjects_split]{fit_mixed_subjects_split()}}.
}
\examples{
pars <- data.frame(a = c(1, 1.2), d = c(0, -0.5))
resp <- matrix(c(1, 0, 0, 1), nrow = 2, byrow = TRUE)
q <- mixed_subjects_quadrature(resp, item_pars = pars, N_quad = 5)
names(q)
}
