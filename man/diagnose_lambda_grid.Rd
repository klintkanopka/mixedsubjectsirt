% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{diagnose_lambda_grid}
\alias{diagnose_lambda_grid}
\title{Diagnose lambda values over a grid}
\usage{
diagnose_lambda_grid(
  lambda_grid,
  observed,
  predicted,
  generated,
  split = FALSE,
  ...
)
}
\arguments{
\item{lambda_grid}{Numeric vector of lambda values in \verb{[0, 1]}.}

\item{observed, predicted, generated}{Response matrices passed to
\code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}}.}

\item{split}{Logical; if \code{TRUE}, call \code{\link[=fit_mixed_subjects_split]{fit_mixed_subjects_split()}}.}

\item{...}{Additional arguments passed to the selected fitting function.}
}
\value{
A list with \code{summary}, \code{lowest_observed_loss_lambda}, and all fitted
model objects.
}
\description{
Fits \code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}} or \code{\link[=fit_mixed_subjects_split]{fit_mixed_subjects_split()}} over a set of
candidate lambda values. The returned summary reports the fitted
mixed-subjects objective and the observed human expected-count loss for each
candidate. This is a sensitivity diagnostic, not a valid tuning rule.
}
\examples{
set.seed(3)
pars <- data.frame(a = c(1, 1.2, 0.9), d = c(0, -0.5, 0.3))
observed <- simulate_2pl(rnorm(30), pars)
predicted <- observed
generated <- simulate_2pl(rnorm(80), pars)
tuned <- diagnose_lambda_grid(
  c(0, 0.5),
  observed, predicted, generated,
  initial_pars = pars, n_quad = 5, control = list(maxit = 30)
)
tuned$summary
}
