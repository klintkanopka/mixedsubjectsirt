% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk-1pl.R
\name{tune_lambda_ppi_score_1pl}
\alias{tune_lambda_ppi_score_1pl}
\title{Plug-in PPI++ optimal tuning parameter for a 1PL model}
\usage{
tune_lambda_ppi_score_1pl(
  observed,
  predicted,
  item_pars,
  n_generated,
  quadrature = NULL,
  n_quad = 31
)
}
\arguments{
\item{observed}{Human response matrix.}

\item{predicted}{Paired binary LLM responses (0/1) for the same rows as
\code{observed}. Probabilities are not accepted; sample binary responses first.}

\item{item_pars}{Item parameters in slope-intercept form at which to
evaluate the score vectors. Typically the human 2PL MLE from \code{\link[=fit_2pl]{fit_2pl()}}.}

\item{n_generated}{Number of generated (unpaired) LLM subjects, used to
compute the ratio \code{r} (\code{n / n_generated}).}

\item{quadrature}{Optional quadrature grid. If omitted, a standard-normal
grid with \code{n_quad} nodes is created.}

\item{n_quad}{Number of quadrature nodes when \code{quadrature} is omitted.}
}
\value{
A list with \code{lambda}, \code{n}, \code{n_generated}, \code{r}, \code{C_hf}, \code{V_f}.
}
\description{
Applies the PPI++ Proposition 2 formula using \code{(J+1)}-dimensional score
vectors for the 1PL parameterization \verb{(a_shared, d_1, ..., d_J)}.
}
\details{
This is the \strong{item-parameter variance} objective — it minimizes
\code{Tr(Sigma_1pl)}. For practical scoring applications use
\code{\link[=tune_lambda_ability_risk_1pl]{tune_lambda_ability_risk_1pl()}} instead.
}
\examples{
set.seed(1)
pars <- data.frame(a = 1, d = c(-0.5, 0, 0.5))
obs  <- simulate_2pl(rnorm(40), pars)
tune_lambda_ppi_score_1pl(obs, obs, pars, n_generated = 100, n_quad = 7)$lambda
}
