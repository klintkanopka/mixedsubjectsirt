% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-1pl.R
\name{fit_mixed_subjects_mml_1pl}
\alias{fit_mixed_subjects_mml_1pl}
\title{Fit a mixed-subjects 1PL calibration via marginal maximum likelihood}
\usage{
fit_mixed_subjects_mml_1pl(
  observed,
  predicted,
  generated,
  lambda = 1,
  n_quad = 31,
  initial_pars = NULL,
  quadrature = NULL,
  mml_pred_weights = c("own", "human"),
  slope_lower = 1e-04,
  slope_upper = NULL,
  control = list(maxit = 500),
  ...
)
}
\arguments{
\item{observed}{Human response matrix, with rows for subjects and columns for
items. Values must be binary when \code{initial_pars} is omitted.}

\item{predicted}{Binary LLM responses (0/1) for the same rows and items as
\code{observed}. Probabilities are not accepted: fractional values are not a
valid likelihood input for the marginal IRT objective and break the PPI
correction, so sample binary responses from any probabilities first (e.g.
\code{rbinom}).}

\item{generated}{Binary generated or unlabeled LLM responses (0/1) for the
same item columns. Probabilities are not accepted (see \code{predicted}).}

\item{lambda}{Power-tuning parameter in \verb{[0, 1]}.}

\item{n_quad}{Number of standard-normal quadrature nodes.}

\item{initial_pars}{Optional starting item parameters. If omitted, a 2PL model
is fit to \code{observed}.}

\item{quadrature}{Optional quadrature grid with \code{theta} and \code{weight} columns.}

\item{mml_pred_weights}{How to compute posteriors for the paired \code{predicted}
term.  \code{"own"} uses posteriors from the predicted responses; \code{"human"}
uses posteriors from the observed human responses.  See Details.}

\item{slope_lower}{Lower bound for discrimination parameters during
optimization. Use \code{NULL} for no lower bound.}

\item{slope_upper}{Upper bound on discrimination parameters. Unlike
\code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}}, this function should not require capping for
well-posed problems because the true marginal objective has no false
minimum at large discrimination.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{...}{Additional arguments passed to \code{\link[=fit_1pl]{fit_1pl()}} when \code{initial_pars}
is omitted.}
}
\value{
An object of class \code{c("mixedsubjects_1pl_fit", "mixedsubjects_fit")}.
}
\description{
Analogous to \code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} but estimates a shared discrimination
parameter \code{a} across all items (1PL model). Posteriors are recomputed at
every gradient evaluation — no frozen-posterior gradient asymmetry.
}
\details{
Only scalar \code{lambda} is supported; per-item lambda is not meaningful for
the 1PL because the discrimination is shared across items.
}
\examples{
set.seed(1)
pars <- data.frame(a = 1, d = c(-0.5, 0, 0.5))
observed  <- simulate_2pl(rnorm(40), pars)
generated <- simulate_2pl(rnorm(100), pars)
fit <- fit_mixed_subjects_mml_1pl(
  observed, observed, generated,
  lambda = 0.5, initial_pars = pars, n_quad = 7,
  control = list(maxit = 100)
)
fit$item_pars
}
\seealso{
\code{\link[=fit_mixed_subjects_1pl]{fit_mixed_subjects_1pl()}} for the frozen expected-count version;
\code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} for the 2PL version.
}
