% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{ability_risk}
\alias{ability_risk}
\title{Propagated ability risk from item-parameter uncertainty}
\usage{
ability_risk(
  resp,
  fit_or_pars,
  vcov = NULL,
  theta_true = NULL,
  bounds = c(-6, 6)
)
}
\arguments{
\item{resp}{Target response matrix.}

\item{fit_or_pars}{A \code{"mixedsubjects_fit"} object or item-parameter data
frame.}

\item{vcov}{Optional covariance matrix. Required when \code{fit_or_pars} is item
parameters rather than a fitted mixed-subjects object.}

\item{theta_true}{Optional true theta values for simulation studies.}

\item{bounds}{Bounds passed to \code{\link[=score_theta]{score_theta()}}.}
}
\value{
A list with \code{summary} and per-pattern \code{details}.
}
\description{
Computes \verb{g_i' Sigma g_i} for each response pattern, where \code{g_i} is the
gradient of the ability estimate with respect to item parameters. If
\code{theta_true} is supplied, the returned total risk also includes squared
ability estimation error.
}
\examples{
set.seed(1)
pars <- data.frame(a = c(1, 1.2), d = c(0, -0.5))
resp <- simulate_2pl(rnorm(30), pars)
Sigma <- diag(0.01, 4)
ability_risk(resp, pars, vcov = Sigma)$summary
}
