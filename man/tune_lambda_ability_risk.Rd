% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{tune_lambda_ability_risk}
\alias{tune_lambda_ability_risk}
\title{Tune lambda by downstream ability-score risk}
\usage{
tune_lambda_ability_risk(
  lambda_grid,
  observed,
  predicted,
  generated,
  target_resp = NULL,
  theta_true = NULL,
  n_quad = 31,
  initial_pars = NULL,
  fit_fn = fit_mixed_subjects,
  bounds = c(-6, 6),
  max_discrimination = 10,
  control = list(maxit = 500),
  ...
)
}
\arguments{
\item{lambda_grid}{Numeric vector of candidate lambda values in \verb{[0, 1]}.}

\item{observed, predicted, generated}{Response matrices passed to
\code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}}.}

\item{target_resp}{Response matrix defining the target scoring population. If
omitted, \code{observed} is used.}

\item{theta_true}{Optional true theta values for \code{target_resp}, used in
simulation studies to add squared scoring error to the risk. When omitted,
\code{mean_squared_error} in the summary is \code{NA}; only \code{mean_param_var} is
computed.}

\item{n_quad}{Number of quadrature nodes.}

\item{initial_pars}{Optional starting item parameters.}

\item{fit_fn}{Fitting function to use. Defaults to \code{\link[=fit_mixed_subjects]{fit_mixed_subjects()}}
(frozen expected-count). Pass \code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} to use the
marginal-likelihood PPI++ estimator, which eliminates gradient asymmetry
and should select \code{lambda > 0} for genuinely informative predictors.}

\item{bounds}{Bounds passed to \code{\link[=score_theta]{score_theta()}}.}

\item{max_discrimination}{Upper bound on plausible item discrimination. Any
candidate fit whose maximum \verb{|a|} exceeds this value is treated as
degenerate and excluded from selection. This guards against runaway
discrimination fits, which can "converge" with a spuriously low model-based
risk (huge discrimination collapses the item-parameter covariance). The
default of 10 is far above any realistic 2PL discrimination.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{...}{Additional arguments passed to \code{fit_fn}.}
}
\value{
A list with \code{summary}, \code{best_lambda}, \code{best_fit}, and all fitted
candidate objects.
}
\description{
Fits candidate mixed-subjects calibrations, estimates the item-parameter
sandwich covariance for each, and chooses the lambda that minimizes average
propagated ability-score risk on a target response matrix.
}
\details{
This function minimizes \verb{E[g' Sigma_gamma g]} — the propagated ability-score
risk — which is the appropriate objective for IRT applications where accurate
test scoring is the goal. This is \strong{distinct} from \code{\link[=tune_lambda_ppi_score]{tune_lambda_ppi_score()}},
which minimizes the trace of the item-parameter covariance matrix
\code{Tr(Sigma_gamma)} (the PPI++ theoretical objective). The two criteria
generally yield different lambda values:
\itemize{
\item \code{tune_lambda_ability_risk()} asks: which lambda produces the most accurate
ability scores for the target population? Use this for operational scoring.
\item \code{\link[=tune_lambda_ppi_score]{tune_lambda_ppi_score()}} asks: which lambda minimizes item-parameter
estimation variance? Use this for method validation and diagnostics.
}

Diagnostic note: if \code{tune_lambda_ability_risk()} selects \code{lambda = 0} for a
misaligned LLM (one whose item parameters differ from the human calibration),
this is the correct mathematical outcome under the current fixed-posterior
expected-count implementation. The frozen posteriors create a gradient
asymmetry that inflates item parameters at any \code{lambda > 0}, increasing
ability risk. This is not a bug in the risk function; it is a property of the
estimating equations. See \code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} for a marginal-likelihood
implementation that removes this asymmetry.
}
\examples{
set.seed(1)
pars <- data.frame(a = c(1, 1.2, 0.9), d = c(0, -0.5, 0.3))
observed <- simulate_2pl(rnorm(40), pars)
generated <- simulate_2pl(rnorm(100), pars)
tuned <- tune_lambda_ability_risk(
  c(0, 0.5), observed, observed, generated,
  initial_pars = pars, n_quad = 5, control = list(maxit = 30)
)
tuned$best_lambda
}
\seealso{
\code{\link[=tune_lambda_ppi_score]{tune_lambda_ppi_score()}} for the PPI++ theoretical lambda that
minimizes the trace of the item-parameter covariance matrix;
\code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} for the marginal-likelihood estimator.
}
