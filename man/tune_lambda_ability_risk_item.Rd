% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk.R
\name{tune_lambda_ability_risk_item}
\alias{tune_lambda_ability_risk_item}
\title{Per-item ability-risk lambda tuning via coordinate descent}
\usage{
tune_lambda_ability_risk_item(
  lambda_grid = seq(0, 1, by = 0.1),
  observed,
  predicted,
  generated,
  target_resp = NULL,
  theta_true = NULL,
  n_quad = 31,
  initial_pars = NULL,
  n_pass = 1,
  init_lambda = 0,
  method = c("optimize", "grid"),
  bounds = c(-6, 6),
  max_discrimination = 10,
  control = list(maxit = 300),
  ...
)
}
\arguments{
\item{lambda_grid}{Numeric vector of candidate λ values in \verb{[0, 1]} to try
for each item independently.}

\item{observed, predicted, generated}{Response matrices passed to
\code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}}.}

\item{target_resp}{Target scoring population. If omitted, \code{observed} is used.}

\item{theta_true}{Optional true theta values, used to add squared scoring
error to the risk.}

\item{n_quad}{Number of quadrature nodes.}

\item{initial_pars}{Optional starting item parameters.}

\item{n_pass}{Number of coordinate-descent passes (default 1).}

\item{init_lambda}{Starting lambda vector for coordinate descent. Supply the
global scalar optimum from \code{\link[=tune_lambda_ability_risk]{tune_lambda_ability_risk()}} (e.g.
\code{init_lambda = 0.5}) to start the search around a useful operating point.
Starting from all-zeros is not recommended: each single-item improvement is
too small to detect when other items are at zero. A scalar is broadcast to
all items; a vector of length \code{n_items} sets per-item starting values.}

\item{method}{How each item's lambda is chosen at a coordinate step:
\code{"optimize"} (default, direct 1-D optimization over \code{range(lambda_grid)},
continuous) or \code{"grid"} (evaluate every value in \code{lambda_grid}).}

\item{bounds}{Bounds passed to \code{\link[=score_theta]{score_theta()}}.}

\item{max_discrimination}{Upper bound on plausible item discrimination; any
candidate fit whose maximum \verb{|a|} exceeds it is treated as degenerate and
skipped. See \code{\link[=tune_lambda_ability_risk]{tune_lambda_ability_risk()}} for the rationale. Default 10.}

\item{control}{Control list passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{...}{Additional arguments passed to \code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}}.}
}
\value{
A list with \code{lambda} (per-item vector), \code{item} (item names),
\code{n_pass}, \code{method}, and \code{final_fit} (the \code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} fit at
the selected lambda).
}
\description{
Finds a per-item vector of lambda values \verb{λ_j ∈ [0, 1]} that minimizes
propagated ability-score risk \verb{E[g' Σ_γ g]} using coordinate descent on the
items. Each coordinate step holds the other \verb{λ_\{j'\}} fixed and selects \code{λ_j}
by direct 1-D optimization (\code{method = "optimize"}, the default, continuous) or
over \code{lambda_grid} (\code{method = "grid"}).
}
\details{
Calls \code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}} with a per-item lambda vector at each
candidate evaluation. Because the lambda is a vector, that function
\strong{switches to its frozen expected-count Q-function path} — posteriors are
frozen at \code{initial_pars}, not recomputed continuously. This is an
approximation; see the \verb{@note} below. The resulting lambda vector can be
used directly with \code{\link[=fit_mixed_subjects_mml]{fit_mixed_subjects_mml()}}.

\strong{Computational cost.} Each pass refits per item per candidate lambda:
\code{method = "grid"} does \verb{n_items × length(lambda_grid)} fits; \code{method = "optimize"} does roughly \verb{n_items × 12} (the optimizer's evaluations plus the
endpoints). Use \code{n_pass = 1} (the default) for a single greedy sweep, which is
usually sufficient.
}
\note{
\strong{Approximation status.} The coordinate descent fits use the frozen
expected-count Q-function (not the full marginal-MML objective) because the
IRT marginal likelihood integrates over the joint response pattern and does
not decompose item-wise. The approach is approximately correct when
\code{initial_pars} is close to the converged parameters. Report per-item
results as experimental / approximate.
}
\examples{
set.seed(1)
pars <- data.frame(a = c(1, 1.2, 0.9), d = c(0, -0.5, 0.3))
observed  <- simulate_2pl(rnorm(40), pars)
generated <- simulate_2pl(rnorm(100), pars)
tuned <- tune_lambda_ability_risk_item(
  c(0, 0.5), observed, observed, generated,
  initial_pars = pars, n_quad = 5, control = list(maxit = 30)
)
tuned$lambda
}
\seealso{
\code{\link[=tune_lambda_ppi_score_item]{tune_lambda_ppi_score_item()}} for the faster PPI++-score version;
\code{\link[=tune_lambda_ability_risk]{tune_lambda_ability_risk()}} for the global scalar version.
}
